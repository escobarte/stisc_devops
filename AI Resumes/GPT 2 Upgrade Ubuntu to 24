### 📋 Резюме: Обновление Ubuntu 20.04 → 24.04 на vSphere

1. **Сделай бэкап / snapshot VM**

2. **Обнови текущую систему**:

   ```bash
   sudo apt update && sudo apt upgrade -y && sudo apt dist-upgrade -y && sudo apt autoremove -y
   ```

3. **Убедись, что установлен `update-manager-core`**:

   ```bash
   sudo apt install update-manager-core
   ```

4. **Проверь конфиг**:
   В `/etc/update-manager/release-upgrades` должно быть `Prompt=lts`

5. **Обновись до Ubuntu 22.04**:

   ```bash
   sudo do-release-upgrade
   ```

6. **После перезагрузки — обновись до Ubuntu 24.04**:

   ```bash
   sudo do-release-upgrade
   ```

7. **Проверь версию**:

   ```bash
   lsb_release -a
   ```

📌 *Если система не предлагает новую версию — используй `sudo do-release-upgrade -d`*

---

Хочешь, дам готовый `.sh` скрипт для автоматизации этих шагов?
